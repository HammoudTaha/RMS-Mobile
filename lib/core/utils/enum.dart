enum BookStatus { active, cancelled, complated }

enum OrderStatus { pending, cancelled, complated }

enum OrderType { delivery, pickUp, dineIn }

enum PaymentMethod { payPal, googlePay, masrtCard, cash }

enum BookType { customized, random }
